import { createSyntheticLeagueSnapshot } from '@keeper/test-fixtures';

// Placeholder until Phase 6 (prompt 10) adds the real React/Vite dashboard.
export function loadPlaceholderSnapshot() {
  return createSyntheticLeagueSnapshot();
}
